// 可以修改这个config，使username 和 password 变成动态的，然后创一个数组建立记录，这样应该可以实现不同用户登录

var config = {
    database: 'DBC_design',
    username: 'root',
    password: 'lovejie',
    host: 'localhost',
    port: 3306
};

module.exports = config;